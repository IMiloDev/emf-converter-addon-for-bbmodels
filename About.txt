Converts Blockbench animations (keyframes) into mathematical expressions compatible with EMF (Entity Model Features).

Supports:
- idle (age-based looping animations)
- walk/run (limb_swing + limb_speed)
- custom variables (user-defined triggers)

Usage:
File → Export animation as .jem

Made for EMF (Entity Model Features)
By IMilo
